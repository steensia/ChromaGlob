package com.asimplenerd.chromaglobs.Game;

/**
 * A holder object to contain the new coordinates for the new objects.
 * May not be needed, but we'll see for now.
 */
public class ReCoordinate {
    // coordinates on a screen
    float x;
    float y;

    // secondary coordinates on the screen
    float x2;
    float y2;

    // the width and height of an object, if applicable
    float w;
    float h;
}
